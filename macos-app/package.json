{
  "name": "cognitive-redline",
  "version": "2.0.0",
  "description": "The Cognitive Redline — privacy-first burnout diagnostics menu-bar app",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc && mkdir -p dist/renderer && cp src/renderer/index.html dist/renderer/",
    "start": "npm run build && electron dist/main.js",
    "dev": "npm run build && electron dist/main.js"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.11.0",
    "electron": "^30.0.0",
    "typescript": "^5.4.0"
  }
}
